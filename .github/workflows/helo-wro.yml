name: hello world
on:
  push:
    branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Set TODAY environment variable
        run: echo "TODAY=$(date +%y-%m-%d)" >> $GITHUB_ENV
      - name: Print hello world with date
        run: echo "hello world, today is $TODAY"